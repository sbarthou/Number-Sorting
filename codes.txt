plt.scatter(x, numbers, s=20)
plt.vlines(x, ymin=0, ymax=numbers, color='black', linewidth=0.7)
plt.show()



m = 0
for i in range(1, len(num)):
    n = i
    if num[0] < num[i] and num[i] not in snum:
        snum[m] = num[i]
        m += 1
        break
for i in range(n+1, len(num)):
    if num[0] < num[i]:
        snum[m] = num[i]
        m += 1
        break